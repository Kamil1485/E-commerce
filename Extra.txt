
  filteredCategories.slice((page-1)*10, (page-1)*10+10)?.filter((item)=>{
            if(searched===""){
                return item;
              }
             else if(item.title.toLocaleLowerCase().includes(searched.toLocaleLowerCase())){
              return item;
             }
        }
        */

        /*
         {uniqueBrands.map((item) => (
                   <option key={item.brand}>{item.brand.toUpperCase()}</option>
                  ))}*/